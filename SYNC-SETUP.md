# 가족 실시간 동기화 붙이기 (Firebase)

부모가 일정을 바꾸면 아이 폰에 **바로 반영되는** 기능을 켜는 방법입니다.
서버 코드를 짤 필요는 없고, 구글 Firebase 콘솔에서 **클릭 몇 번 + 값 두 개 복사**면 됩니다.
카드 등록 없이 무료 요금제로 충분합니다.

> 앱 안에서도 같은 안내를 볼 수 있습니다 — `메뉴 → 가족 그룹 · 동기화`

---

## 1. Firebase 프로젝트 만들기

1. <https://console.firebase.google.com> 접속 후 구글 로그인
2. **프로젝트 만들기** 클릭
3. 프로젝트 이름 입력 (예: `kuma-routine`) → 계속
4. **Google 애널리틱스는 꺼도 됩니다** → 프로젝트 만들기
5. 1분쯤 기다리면 완료

## 2. Realtime Database 만들기

> Firestore 가 아니라 **Realtime Database** 입니다. 헷갈리기 쉬우니 주의하세요.

1. 왼쪽 메뉴 **빌드 → Realtime Database**
2. **데이터베이스 만들기**
3. 위치는 아무거나 (`asia-southeast1` 권장)
4. 보안 규칙은 **잠금 모드로 시작** 선택 → 사용 설정

## 3. 익명 로그인 켜기

1. 왼쪽 메뉴 **빌드 → Authentication**
2. **시작하기**
3. 로그인 방법 탭에서 **익명** 선택 → 사용 설정 → 저장

이걸 켜야 앱이 계정 없이도 서버에 접근할 수 있습니다.
아이에게 이메일·비밀번호를 만들게 하지 않아도 됩니다.

## 4. 보안 규칙 넣기

**Realtime Database → 규칙** 탭에서 내용을 아래로 바꾸고 **게시**:

```json
{
  "rules": {
    "groups": {
      "$code": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
```

로그인한 사람만 접근할 수 있고, 그룹은 8자리 코드를 아는 사람만 찾을 수 있습니다.

> **한계를 알고 쓰세요.** 이 규칙은 "로그인한 누구나 아무 그룹이나 읽을 수 있다"는 뜻입니다.
> 코드를 모르면 사실상 찾을 수 없지만(32^8 ≈ 1조 가지), 암호학적으로 안전한 건 아닙니다.
> 아이 이름과 학원 시간이 들어가는 데이터이니, 코드를 공개된 곳에 올리지 마세요.
> 더 엄격히 막으려면 멤버 uid 기반 규칙이 필요하고, 그건 가입 절차가 한 단계 늘어납니다.

## 5. 설정값 두 개 복사

1. 왼쪽 위 **⚙️ → 프로젝트 설정**
2. 아래로 스크롤 → **내 앱** → 웹 아이콘 `</>` 클릭
3. 앱 닉네임 아무거나 입력 → 앱 등록 (호스팅은 체크 안 해도 됨)
4. 나오는 코드에서 **두 줄만** 복사합니다

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",                              // ← 이것
  databaseURL: "https://xxx.firebasedatabase.app",  // ← 이것
  ...
};
```

> `databaseURL` 이 안 보이면 2단계(Realtime Database)를 건너뛴 것입니다.
> 만들고 나면 자동으로 생깁니다.

## 6. 앱에 넣기

1. 앱에서 `메뉴 → 가족 그룹 · 동기화`
2. **apiKey** 와 **databaseURL** 붙여넣기 → **저장하고 연결하기**
3. 연결되면 **새 가족 그룹 만들기** → `ABCD-1234` 같은 코드가 나옵니다
4. 같이 쓸 사람에게 그 코드를 알려주고, 상대방 앱에서 **코드로 참여하기**

끝입니다. 이제 한쪽에서 일정을 바꾸면 다른 기기에 바로 반영됩니다.

---

## 동작 방식

- 외부 SDK 를 넣지 않고 **Firebase REST API + EventSource** 로 직접 붙였습니다.
  단일 HTML 파일 구조가 그대로 유지됩니다.
- 익명 로그인으로 기기마다 uid 를 받고, `groups/<코드>` 한 곳에 상태를 저장합니다.
- 로컬에서 무언가 바뀌면 **0.7초 묶어서** 한 번만 올립니다(연타 방지).
- 서버가 바뀌면 EventSource 로 즉시 받아 화면을 다시 그립니다.
  내가 올린 변경이 되돌아온 경우는 `by` 필드로 걸러냅니다.
- 충돌은 **나중에 쓴 쪽이 이깁니다**(last-write-wins). 부모와 아이가
  같은 순간 같은 일정을 고치는 경우는 드물어 이 정도로 충분합니다.
  동시 편집이 잦아지면 필드 단위 병합이 필요합니다.

## 설정하지 않아도

동기화를 켜지 않으면 앱은 **지금까지처럼 기기 안에서만** 동작합니다.
서버 연결 실패가 앱 사용을 막지 않습니다.

한 번만 옮기고 싶을 땐 `메뉴 → 코드로 내보내기` 로 상태를 문자열로 복사해
카톡으로 보내는 방법도 그대로 남아 있습니다(백업·기기 이전용).

## 비용

Realtime Database 무료(Spark) 요금제: 저장 1GB, 다운로드 월 10GB.
가족 한 팀의 일정 데이터는 수십 KB 수준이라 **수천 가족이 써도 무료 범위**입니다.
