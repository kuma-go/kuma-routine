# 실시간 동기화 켜기 — **운영자가 한 번만** 하는 설정

> 이 문서는 **앱을 배포하는 사람**을 위한 것입니다.
> 앱을 쓰는 가족들은 아무것도 설정하지 않습니다. 초대 코드만 주고받으면 됩니다.

동기화는 여러 기기가 함께 보는 **공용 저장소**가 있어야 동작합니다.
운영자가 Firebase 프로젝트를 하나 만들고 그 값을 소스에 넣어 배포하면,
그 앱을 쓰는 **모든 사용자가 설정 없이** 그룹을 만들고 참여할 수 있습니다.

```
운영자 (한 번)                     사용자 (매번)
────────────────                   ─────────────
Firebase 프로젝트 생성        →     앱 열기
apiKey · databaseURL 복사     →     메뉴 → 가족 그룹 · 동기화
src/mod_sync.js 에 붙여넣기   →     새 가족 그룹 만들기
빌드 후 배포                  →     코드 공유 → 상대가 코드로 참여
```

---

## 1. Firebase 프로젝트 만들기

1. <https://console.firebase.google.com> 접속 후 구글 로그인
2. **프로젝트 만들기** → 이름 입력 (예: `kuma-routine`)
3. Google 애널리틱스는 꺼도 됩니다 → 만들기

## 2. Realtime Database 만들기

> Firestore 가 아니라 **Realtime Database** 입니다. 헷갈리기 쉬우니 주의하세요.

1. 왼쪽 메뉴 **빌드 → Realtime Database → 데이터베이스 만들기**
2. 위치는 아무거나 (`asia-southeast1` 권장)
3. **잠금 모드로 시작** 선택 → 사용 설정

## 3. 보안 규칙 넣기

만들어진 뒤 상단 **규칙** 탭에서 아래로 바꾸고 **게시**:

```json
{
  "rules": {
    "groups": {
      "$code": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
```

## 4. 익명 로그인 켜기

1. 왼쪽 메뉴 **빌드 → Authentication → 시작하기**
2. 로그인 방법 탭 → **익명** → 사용 설정 → 저장

**이 단계를 빠뜨리면** 위 규칙의 `auth != null` 을 통과하지 못해
앱이 `익명 로그인이 꺼져 있어요` 라고 표시합니다. 가장 흔한 실수입니다.

## 5. 값 두 개 복사

1. 왼쪽 위 **⚙️ → 프로젝트 설정**
2. 아래로 스크롤 → **내 앱** → 웹 아이콘 `</>` → 닉네임 입력 후 등록
3. 나오는 코드에서 두 줄만 복사

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",                              // ← 이것
  databaseURL: "https://xxx.firebasedatabase.app",  // ← 이것
};
```

## 6. GitHub Secrets 에 넣기 — **소스에 직접 쓰지 않습니다**

키를 저장소에 커밋하지 않습니다. 배포할 때만 주입합니다.

1. GitHub 저장소 → **Settings → Secrets and variables → Actions**
2. **New repository secret** 로 두 개 등록

| Name | Secret |
|---|---|
| `FIREBASE_API_KEY` | `AIzaSy...` |
| `FIREBASE_DB_URL` | `https://xxx.firebasedatabase.app` |

## 7. Pages 배포 방식 바꾸기

기존의 "브랜치에서 바로 서빙" 방식은 저장소 파일을 그대로 내보내므로
주입이 끼어들 자리가 없습니다. Actions 방식으로 바꿉니다.

**Settings → Pages → Source** 를 **GitHub Actions** 로 변경

이미 `.github/workflows/deploy.yml` 이 들어 있습니다. `main` 에 푸시하면

```
checkout → 시크릿 주입 → 빌드 → _site 만 배포
```

순서로 동작합니다. `src/` 는 배포에서 제외합니다 — 주입된 키가
소스 파일에 남아 있기 때문입니다. 배포되는 건 `index.html` 과
아이콘·manifest·sw 뿐입니다.

끝입니다. 이제 사용자는 `메뉴 → 가족 그룹 · 동기화` 에서
**바로 그룹을 만들 수 있습니다.** 설정 화면은 보이지 않습니다.

> 저장소의 `KUMA_FIREBASE` 는 계속 비어 있습니다.
> 로컬에서 열면 "각자 서버를 넣는 개발자 모드"로 동작하니 테스트에 쓰세요.

---

## 키 취급 원칙

Firebase 웹 `apiKey` 는 기술적으로는 비밀번호가 아니라 **프로젝트 식별자**이고,
클라이언트에서 보이는 것을 전제로 설계돼 있습니다.

그렇다고 **공개 저장소에 커밋해도 되는 건 아닙니다.**

- 봇이 공개 저장소를 스캔해 키를 수집합니다
- 리퍼러 제한을 걸기 전 공백 동안 남용될 수 있습니다
- 교체하려면 커밋 히스토리까지 정리해야 합니다

그래서 이 저장소는 **소스에 키를 두지 않고 배포 시에만 주입**합니다.
배포된 `index.html` 에는 들어가므로 브라우저에서는 보입니다 —
클라이언트 직접 연결 방식의 구조적 한계이며, 아래 보호 장치로 막습니다.

실제 보호는 두 가지로 합니다.

### ① 보안 규칙 (필수 · 위 3단계에서 완료)

이 규칙은 "로그인한 사람은 코드를 알면 접근 가능"입니다.
그룹 코드가 8자리(32⁸ ≈ 1조 가지)라 무작위로는 사실상 못 찾습니다.
다만 암호학적으로 안전하진 않으니 **코드를 공개된 곳에 올리지 마세요.**

### ② API 키 제한 (**필수**)

키가 공개되므로 남이 자기 사이트에서 갖다 쓸 수 있습니다.
막으려면 **호출 출처를 제한**하세요.

1. <https://console.cloud.google.com/apis/credentials> 접속 (같은 구글 계정)
2. 프로젝트 선택 → `Browser key (auto created by Firebase)` 클릭
3. **애플리케이션 제한사항 → HTTP 리퍼러(웹사이트)** 선택
4. 아래 항목 추가

```
https://kuma-go.github.io/*
```

로컬 테스트도 한다면 `http://localhost:*/*` 를 함께 넣으세요.
이렇게 하면 배포본에서 키를 복사해가도 다른 사이트에서는 동작하지 않습니다.
**이 설정을 하지 않으면 키가 사실상 무방비입니다. 배포 전에 반드시 하세요.**

### ③ 키를 아예 노출하지 않으려면

브라우저에서도 키가 보이지 않게 하려면 **프록시**가 필요합니다.
Cloudflare Workers 같은 곳에 키를 두고 앱은 프록시만 호출하는 구조입니다.
가장 안전하지만 배포 대상이 하나 늘어납니다. 필요하면 작업 요청해 주세요.

### ④ 그 밖에

- **App Check** (reCAPTCHA) 를 붙이면 봇 접근을 차단할 수 있습니다.
- 보안 규칙을 멤버 uid 기반으로 바꾸면 코드를 알아도 초대받지 않은
  사람은 못 들어옵니다. 대신 참여 절차가 한 단계 늘어납니다.

---

## 비용

Realtime Database 무료(Spark) 요금제: 저장 1GB · 다운로드 월 10GB · 동시접속 100.

가족 한 팀의 데이터는 수십 KB 수준이라 **수백 가족까지는 무료 범위**입니다.
동시접속 100 이 먼저 한계에 닿습니다. 넘어가면 Blaze 요금제(사용한 만큼)로
전환하면 되고, 이 규모에서는 월 몇 천 원 수준입니다.

사용량은 Firebase 콘솔 → Realtime Database → **사용량** 탭에서 볼 수 있습니다.

---

## 동작 방식

- 외부 SDK 없이 **Firebase REST API + EventSource** 로 붙였습니다.
  단일 HTML 파일 구조가 그대로 유지됩니다.
- 익명 로그인으로 기기마다 uid 를 받고 `groups/<코드>` 한 곳에 상태를 저장합니다.
- 로컬 변경은 **0.7초 묶어서** 한 번만 올립니다(연타 방지).
- 서버 변경은 EventSource 로 즉시 받아 화면을 다시 그립니다.
  내가 올린 변경이 되돌아온 경우는 `by` 필드로 걸러냅니다.
- 충돌은 **나중에 쓴 쪽이 이깁니다**(last-write-wins).
- 연결이 끊기면 6초 뒤 토큰을 새로 받아 재연결합니다.

## 서버 없이도 동작합니다

동기화를 켜지 않거나 서버 연결이 실패해도 앱은 **기기 안에서 정상 동작**합니다.
`메뉴 → 코드로 내보내기` 로 상태를 문자열로 옮기는 방법도 그대로 남아 있습니다
(백업 · 기기 이전용).
